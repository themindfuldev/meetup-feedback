// CONFIG
// ======
module.exports.dev = {

  // port to start the app on
  port: 8000,

  // this could be anything, I chose to use the date
  sessionSecret: ''+new Date().getTime(),

  // config options for the Mongoose connection
  db: 'localhost:27017/meetup-feedback'

};
module.exports.test = {

  // port to start the app on
  port: 8000,

  // this could be anything, I chose to use the date
  sessionSecret: ''+new Date().getTime(),

  // config options for the Mongoose connection
  db: 'localhost:27018/meetup-feedback'

};
module.exports.prod = {

  // port to start the app on
  port: 8000,

  // this could be anything, I chose to use the date
  sessionSecret: ''+new Date().getTime(),

  // config options for the Mongoose connection
  db: 'user:pass@mongodb:27017/beardme'

};
